import { bucleInterval } from "./interval";
import { tecnologyImg } from "./img";
import { checkInputValues, cleanInputs } from "./inputs";

export { bucleInterval, tecnologyImg, checkInputValues, cleanInputs };
